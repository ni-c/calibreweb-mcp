import{_t as e,it as t,n,rt as r}from"./chunks/framework.D9LADW4G.js";var i=JSON.parse(`{"title":"Changelog","description":"","frontmatter":{},"headers":[],"relativePath":"reference/changelog.md","filePath":"reference/changelog.md","lastUpdated":1787093302000}`),a={name:`reference/changelog.md`};function o(n,i,a,o,s,c){return e(),r(`div`,null,[...i[0]||=[t(`<h1 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to “Changelog”">​</a></h1><h2 id="_0-3-1-2026-09-07" tabindex="-1"><a href="https://github.com/ni-c/calibreweb-mcp/releases/tag/v0.3.1" target="_blank" rel="noreferrer">0.3.1</a> - 2026-09-07 <a class="header-anchor" href="#_0-3-1-2026-09-07" aria-label="Permalink to “0.3.1 - 2026-09-07”">​</a></h2><h3 id="security" tabindex="-1">Security <a class="header-anchor" href="#security" aria-label="Permalink to “Security”">​</a></h3><ul><li>Markup is taken out of a book description in one forward pass instead of by re-running a regular expression until the text stops changing. The old shape was quadratic in the input: a run of unclosed <code>&lt;</code> bought one full rescan per character, and 16 096 of them — exactly what the content slice admits — cost 168 ms <strong>per book</strong>, on the thread that serves every request, from anybody who can write a description into the library. A search answer carries hundreds of books. The scan never removes a <code>&lt;</code> that could pair up with a later <code>&gt;</code>, so one pass is the fixpoint by construction rather than by repetition, and every removal emits a separator so two halves cannot become one token. <code>test/linear-time.test.ts</code> holds it, and every other function that reads feed or configuration text, to a budget at the largest input the code accepts.</li><li>The status of a response is decided before its body is read. Every verb read the body under the success ceiling first, so a 401 behind a reverse proxy answering with a two-megabyte login page surfaced as &quot;returned a response larger than 1048576 bytes and was refused&quot; — the size instead of the status, no hint about the credentials, and none of the handling that keys on 401 ever running. Error bodies now have their own 64 KiB ceiling that cuts instead of refusing.</li><li>A refused login is remembered for ten seconds and repeated from memory rather than retried. Calibre-Web logs every refused OPDS login at warning level — <code>OPDS Login failed for user &quot;%s&quot; IP-address: %s</code>, the line intrusion detection filters on — and does not rate-limit them: the limiter call in its <code>verify_password</code> is commented out and the OPDS routes carry none. Every tool here is annotated read-only, idempotent and cheap, which is what a model retries after being told to check the credentials. The record is per process and says when the next real attempt is possible.</li><li>A cover&#39;s media type is read from the image data instead of from the response header, and data matching none of the four signatures is refused. An HTML login page served with <code>image/png</code> reached the client as an image before. Reading the data is also what makes the reported type correct: Calibre names every cover file <code>cover.jpg</code> whatever the image is, and Calibre-Web guesses the header from that name — the integration fixtures are PNG files under that name, which is how a header-versus-data check was caught refusing legitimate covers.</li><li>A character reference naming a surrogate decodes to U+FFFD, and every string is repaired after every cut. <code>String.fromCodePoint(0xd800)</code> answers half a character without complaining, and a summary or a field cut at its budget can split a pair — legal JSON on the wire that raises <code>UnicodeEncodeError</code> in a client encoding it to UTF-8.</li><li>Startup diagnostics no longer print the value of <code>CALIBRE_WEB_URL</code>. A value that fails to parse is the one most likely to be a password pasted one line too high, and redacting userinfo does nothing for a bare one; it is now described by length unless it contains <code>://</code>. The scheme is no longer echoed either — a 56-character hexadecimal key with a colon after it is a valid URL whose scheme is the key.</li><li>The publish job installs with <code>--ignore-scripts</code>. It holds an OIDC token for npm Trusted Publishing, and an install hook of any dependency would have run while that token was available. <code>mcp-publisher</code> is pinned to v1.8.1 with its published checksum instead of <code>releases/latest/download</code>, in both jobs that hold a token, and <code>gh release create</code> verifies the tag.</li><li>Pull requests get a dependency review (<code>fail-on-severity: high</code>). <code>npm audit</code> checks the tree as it is; this checks the change.</li><li>The runtime image drops yarn and corepack as well as npm, and no longer carries <code>package-lock.json</code>, which nothing reads at runtime.</li></ul><h3 id="fixed" tabindex="-1">Fixed <a class="header-anchor" href="#fixed" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>An id the feed states can no longer take a whole listing down. <code>Number</code> on an unbounded digit run answers <code>1e20</code> at twenty digits and <code>Infinity</code> at four hundred; <code>shapedBook.id</code> is <code>z.number()</code> and <code>pagination.nextOffset</code> is <code>z.number().int()</code>, both of which refuse those, and the SDK answers a schema violation with an error for the <em>entire</em> call — so one poisoned entry in a page of fifty made every book in it unreachable, with a message naming no cause. Ids are now read as at most ten digits and must be safe integers inside the range Calibre-Web&#39;s own routes accept; anything else is reported as no id, with a note. The same for the next-page offset, which stops pagination rather than offering an offset nothing can use.</li><li>A counter from <code>/opds/stats</code> that is not finite is dropped rather than answered. <code>typeof value === &#39;number&#39;</code> was the whole check, and <code>1e999</code> in the JSON parses to <code>Infinity</code>, which passes it and fails the output schema.</li><li>One entry can no longer make a listing unanswerable. A metadata field is cut at 1000 characters and the authors, tags, languages and formats one entry contributes are capped, each with a note; a link longer than 2048 characters is dropped. The result ceiling drops summaries and then refuses, so a megabyte title — which is not a summary — used to answer &quot;narrow the request&quot; for every offset that paged over that book, and no narrowing helped.</li><li>The result ceiling measures the text that is actually sent. It measured the compact serialisation and emitted the indented one, which is two to three times as many characters, so the limit held for a string nobody received.</li><li>An upstream error body is labelled as untrusted text from the instance and cut at 200 characters rather than 2000.</li><li><code>CALIBRE_WEB_URL</code> is stored as the parsed origin and path rather than as the environment string, so a query or fragment cannot be glued in front of every request path; trailing slashes come off with an index walk rather than a pattern that is retried from every position of the run.</li></ul><h3 id="added" tabindex="-1">Added <a class="header-anchor" href="#added" aria-label="Permalink to “Added”">​</a></h3><ul><li>The server introduces itself in full. <code>title</code>, <code>description</code>, <code>websiteUrl</code> and <code>icons</code> now travel with <code>name</code> and <code>version</code>, so a client that shows a server to a person has something to show. All four were already in <code>server.json</code> for the registry and reached no client at all; a test compares the two so they cannot drift.</li><li>Server <code>instructions</code>. Results carry an <code>untrusted</code> marker, but that is read after the fact — this is the channel a model sees before it calls anything.</li><li>An OpenSSF Scorecard run, weekly and on every push to <code>main</code>, reporting into the Security tab next to CodeQL and Trivy. The badge is the second in the row.</li><li>A property test drives every feed-reading tool through a connected client over feeds built from hostile field values, and asserts that no answer carries <code>Output validation error</code>, <code>Cannot read properties</code> or <code>is not a function</code>, and that both channels always agree. It reproduced the id defect above on its first run. <code>SHAPE_RUNS</code> raises the run count for a deep local pass.</li><li>The test client now calls <code>tools/list</code> once before every suite, so the client-side schema check runs on every success path rather than only on the paths a test happened to list on. A test drives the insecure-TLS switch, which had never had one.</li></ul><h3 id="changed" tabindex="-1">Changed <a class="header-anchor" href="#changed" aria-label="Permalink to “Changed”">​</a></h3><ul><li>oxlint&#39;s <code>suspicious</code> category is on; 22 findings fixed (mostly <code>Array#toSorted()</code> over copy-and-sort and un-shadowed names). No runtime behaviour changed.</li><li>The tool reference marks the <code>essential</code> preset and the tools that ask a person before they act, per tool rather than only in the introduction. A test keeps both sets in step with the code.</li><li><code>homepage</code> in <code>package.json</code> points at the documentation site rather than at the README anchor on GitHub. It is what npm shows next to the package, and every one of these servers has had a documentation site for weeks.</li><li>Source maps are no longer published in the npm tarball. Node reads them only under <code>--enable-source-maps</code>, which nothing here sets, and the maps pointed at a <code>src/</code> this package does not ship — so a stack trace under that flag named a file nobody could open. <code>dist/**/*.js</code> is unchanged; the package is about a fifth smaller.</li><li><code>letter</code> and <code>offset</code> carry ceilings, and <code>tsconfig.json</code> targets the ES2024 library for <code>String#toWellFormed</code>.</li></ul><h2 id="_0-3-0-2026-09-03" tabindex="-1"><a href="https://github.com/ni-c/calibreweb-mcp/releases/tag/v0.3.0" target="_blank" rel="noreferrer">0.3.0</a> - 2026-09-03 <a class="header-anchor" href="#_0-3-0-2026-09-03" aria-label="Permalink to “0.3.0 - 2026-09-03”">​</a></h2><h3 id="added-1" tabindex="-1">Added <a class="header-anchor" href="#added-1" aria-label="Permalink to “Added”">​</a></h3><ul><li><p>Every tool declares an <code>outputSchema</code> and answers with <code>structuredContent</code> beside the text block. A client no longer has to parse prose to use a result.</p><p>The untrusted-content warning travels with it as <code>untrusted: true</code> and <code>source: &quot;calibre-web&quot;</code> fields, not only as a line in <code>notes</code> — a client that reads the structured half should be able to check the framing rather than find it in a list of sentences. <code>get_stats</code> and <code>get_cover</code> do not carry it: four counters checked to be numbers, and an id with a media type from a four-entry allowlist.</p><p>What comes out of an OPDS feed is described exactly, because this server shapes every field of it itself rather than passing the document on. The book and feed types are now derived from those schemas, so the two cannot drift — a drift would have surfaced as a failed tool call rather than a type error.</p></li></ul><h3 id="changed-1" tabindex="-1">Changed <a class="header-anchor" href="#changed-1" aria-label="Permalink to “Changed”">​</a></h3><ul><li><p>The advertised schemas avoid a spelling that is legal JSON Schema and still gets a tool refused, or its constraint silently dropped, by some MCP clients: a nullable field is written as <code>anyOf</code> branches rather than <code>&quot;type&quot;: [&quot;string&quot;, &quot;null&quot;]</code>, which several clients read as a single type and then drop. What the tools accept and return is unchanged; only the way the schema says so is.</p></li><li><p>A result that is still over the ceiling after book summaries are dropped is now an <strong>error</strong> rather than JSON cut at the ceiling. The truncated form was unparseable, which a text block tolerates and <code>structuredContent</code> cannot — and the two channels have to carry the same value.</p></li><li><p>Runs on <strong>MCP SDK 2.0</strong>. Existing clients see the same protocol revision they always did; the change is the package layout behind it.</p></li><li><p>The linter is <strong>oxlint</strong> instead of eslint plus typescript-eslint, which lifts the TypeScript ceiling: typescript-eslint pins <code>typescript</code> below 6.1, so this repository was held on TypeScript 6 by its linter rather than by its code.</p></li><li><p>The tool filter, the host classifier and the documentation-asset generator now come from <strong><code>mcp-tool-allowlist</code></strong>, <strong><code>mcp-internal-hosts</code></strong> and <strong><code>svg-asset-set</code></strong> rather than from copies kept here — 674 fewer lines, and one place to fix each. None of them has a runtime dependency of its own.</p></li><li><p>stdio is served through <code>serveStdio</code>, so the connection&#39;s era is negotiated on the opening exchange rather than assumed. A client that pins the <code>2026-07-28</code> era is served it; until now its <code>server/discover</code> probe was answered with &quot;Method not found&quot; and only <code>2025-11-25</code> was on offer. A client that speaks the older era sees no change — it is still pinned to one instance for the life of the connection, exactly as a hand-wired <code>StdioServerTransport</code> served it.</p></li></ul><h3 id="fixed-1" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-1" aria-label="Permalink to “Fixed”">​</a></h3><ul><li><p><strong>Four output fields no longer carry control characters into the model context.</strong> <code>uuid</code>, <code>published</code>, <code>updated</code> and a format&#39;s <code>mimeType</code> reached the result verbatim while <code>title</code>, <code>authors</code>, <code>publisher</code>, <code>languages</code>, <code>tags</code>, <code>format</code> and <code>series</code> were being cleaned — so the assurance in <code>decodeXmlText</code>&#39;s docstring, that library metadata is safe for the model and for any terminal rendering it, did not hold for them. A BiDi override such as U+202E reverses the display order of everything after it, which is the Trojan-Source trick; <code>uuid</code> in particular is a plain text column in Calibre, and an imported <code>metadata.db</code> fills it with whatever it likes.</p><p>The strip now happens in <code>optionalText</code>, the funnel every plain metadata field already goes through, rather than field by field — a guard that has to be remembered per field is a guard that gets forgotten, which is exactly how these four were missed.</p></li><li><p>An entry in <code>CALIBRE_WEB_ALLOW_TOOLS</code> that is not tool-name-shaped is now <strong>redacted</strong> in the error rather than quoted back. <code>CALIBRE_WEB_PASSWORD</code> and <code>CALIBRE_WEB_ALLOW_TOOLS</code> are adjacent lines in every compose file, and a paste into the wrong one used to print the credential into the client&#39;s log.</p></li></ul><h2 id="_0-2-0-2026-08-27" tabindex="-1"><a href="https://github.com/ni-c/calibreweb-mcp/releases/tag/v0.2.0" target="_blank" rel="noreferrer">0.2.0</a> - 2026-08-27 <a class="header-anchor" href="#_0-2-0-2026-08-27" aria-label="Permalink to “0.2.0 - 2026-08-27”">​</a></h2><h3 id="added-2" tabindex="-1">Added <a class="header-anchor" href="#added-2" aria-label="Permalink to “Added”">​</a></h3><ul><li><p><code>CALIBRE_WEB_ALLOW_TOOLS</code> and <code>CALIBRE_WEB_DENY_TOOLS</code> choose which of the 6 tools are registered. Both take comma-separated tool names or a prefix with a trailing <code>*</code>, the allow list decides what is in and the deny list is subtracted from it, and <code>CALIBRE_WEB_ALLOW_TOOLS=essential</code> selects a curated five — <code>search_books</code>, <code>list_books</code>, <code>list_shelves</code>, <code>get_shelf_books</code>, <code>get_stats</code>. A model picks the right tool far more reliably from five than from six, and every visible tool costs context on every request. Nothing changes for an installation that sets neither.</p><p>A filtered tool is not registered at all, so it is absent from <code>tools/list</code> and answers <code>tools/call</code> with &quot;tool not found&quot;.</p><p>An entry that matches no tool <strong>stops the server at startup</strong>, naming the entry and listing the real names, rather than being ignored: an ignored typo leaves a tool missing from <code>tools/list</code> with nothing pointing at the cause.</p></li></ul><h3 id="changed-2" tabindex="-1">Changed <a class="header-anchor" href="#changed-2" aria-label="Permalink to “Changed”">​</a></h3><ul><li>The README now carries the same eight badges, in the same order, as every other MCP server in this family, all of them reading from npm rather than hard-coded; the opening follows one shape; and the standalone &quot;Full documentation&quot; line is gone, because the docs badge three lines above it points at the same page.</li></ul><h3 id="fixed-2" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-2" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>The container image no longer ships OpenSSL 3.5.7-r0, which carries <strong>CVE-2026-14456</strong> (denial of service via unbounded memory growth). The pinned <code>node:24-alpine</code> digest is already the newest one; Alpine&#39;s fixed 3.5.8-r0 has simply not been rebuilt into it yet, so the runtime stage now upgrades <code>libcrypto3</code> and <code>libssl3</code> by name. Upgrading those two rather than running a blanket <code>apk upgrade</code> keeps the rest of the image exactly as the digest pins it. The step can go once the base image ships the fix.</li></ul><h2 id="_0-1-3-2026-08-26" tabindex="-1"><a href="https://github.com/ni-c/calibreweb-mcp/releases/tag/v0.1.3" target="_blank" rel="noreferrer">0.1.3</a> - 2026-08-26 <a class="header-anchor" href="#_0-1-3-2026-08-26" aria-label="Permalink to “0.1.3 - 2026-08-26”">​</a></h2><h3 id="changed-3" tabindex="-1">Changed <a class="header-anchor" href="#changed-3" aria-label="Permalink to “Changed”">​</a></h3><ul><li>The check that decides whether <code>CALIBREWEB_URL</code> points somewhere local — and therefore whether sending a credential over plain <code>http</code> is worth warning about — now uses the same host classifier as the other MCP servers in this family, in <code>src/hosts.ts</code>. The string comparison it replaces missed several spellings of the same address: <code>http://[::ffff:127.0.0.1]</code>, which <code>URL</code> canonicalises to <code>[::ffff:7f00:1]</code> before any check sees it, and <code>localhost.</code> with its root label. It also treated <code>127.example.com</code> as loopback, because it matched on the <code>127.</code> prefix, and so stayed quiet about a plain-http URL to a public host.</li></ul><p>Nothing else changes: this server has no tool that takes a URL, so there is no request whose target a caller can choose.</p><h2 id="_0-1-2-2026-08-19" tabindex="-1"><a href="https://github.com/ni-c/calibreweb-mcp/releases/tag/v0.1.2" target="_blank" rel="noreferrer">0.1.2</a> - 2026-08-19 <a class="header-anchor" href="#_0-1-2-2026-08-19" aria-label="Permalink to “0.1.2 - 2026-08-19”">​</a></h2><h3 id="fixed-3" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-3" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>Tag stripping in summaries now runs to a fixpoint, so overlapping constructs like <code>&lt;&lt;script&gt;script&gt;</code> can no longer reassemble into markup (CodeQL <code>js/incomplete-multi-character-sanitization</code>). Defense-in-depth: the output is plain text, but an MCP client rendering results as markdown could have interpreted leftover HTML.</li></ul><h2 id="_0-1-1-2026-08-19" tabindex="-1"><a href="https://github.com/ni-c/calibreweb-mcp/releases/tag/v0.1.1" target="_blank" rel="noreferrer">0.1.1</a> - 2026-08-19 <a class="header-anchor" href="#_0-1-1-2026-08-19" aria-label="Permalink to “0.1.1 - 2026-08-19”">​</a></h2><h3 id="added-3" tabindex="-1">Added <a class="header-anchor" href="#added-3" aria-label="Permalink to “Added”">​</a></h3><ul><li>Documentation site at <a href="https://calibreweb-mcp.ni-c.de" target="_blank" rel="noreferrer">calibreweb-mcp.ni-c.de</a> with a full tools reference and a reproducible demo recording (the demo runs against a bundled fixture library, no Calibre-Web instance needed).</li><li>Release pipeline: npm publishing via Trusted Publishing with provenance, GitHub releases from the changelog, MCP registry submission, and a multi-arch container image on GHCR with SBOM and build provenance.</li></ul><h2 id="_0-1-0-2026-08-19" tabindex="-1"><a href="https://github.com/ni-c/calibreweb-mcp/commit/977ef347" target="_blank" rel="noreferrer">0.1.0</a> - 2026-08-19 <a class="header-anchor" href="#_0-1-0-2026-08-19" aria-label="Permalink to “0.1.0 - 2026-08-19”">​</a></h2><h3 id="added-4" tabindex="-1">Added <a class="header-anchor" href="#added-4" aria-label="Permalink to “Added”">​</a></h3><ul><li>Initial release: read-only MCP server for Calibre-Web via its OPDS feed.</li><li>Tools: <code>search_books</code>, <code>list_books</code> (new/hot/rated/discover/read/unread/all), <code>list_shelves</code>, <code>get_shelf_books</code>, <code>get_cover</code> (image content), <code>get_stats</code>.</li><li>HTTP Basic auth with support for anonymous-browsing instances.</li><li>Hardened XML pipeline: DOCTYPE/entity refusal, no entity processing, bounded response bodies, control-character stripping, credential redaction.</li></ul>`,37)]])}var s=n(a,[[`render`,o]]);export{i as __pageData,s as default};